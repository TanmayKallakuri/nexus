You are working inside a local project folder for a final-test prediction task.

Your job is to generate respondent-level predictions for the final test, using the existing ML outputs as a prior and improving correlation, especially on the weak final-test blocks.

Important:
- Focus only on the final test in this folder.
- Do not optimize for any previous 11-question proxy task.
- Correlation is more important than accuracy.
- Use the existing ML prediction as a prior, not as a hard rule.
- Keep respondent answers internally consistent across related questions.
- Do not collapse everyone toward the middle.

Files you must use:
- `final_test_questions.json`
- `data/personas_text/`
- `CLAUDE_BLEND_PLAYBOOK.md`
- `CLAUDE_PROMPT_TEMPLATE.md`
- `CLAUDE_WEAK_BLOCK_INSTRUCTIONS.md`
- `artifacts/final_test_ml_predictions.json` if it exists
- `artifacts/claude_blend_seed.jsonl` if it exists

Files that explain the local pipeline:
- `build_family_aware_submission.py`
- `blend_claude_with_ml.py`
- `README.md`

What to do:
1. Read the playbook and weak-block instructions first.
2. Read the final test questions.
3. Read the persona text files.
4. If `artifacts/claude_blend_seed.jsonl` exists, use it as the main compact input for respondent-question prediction.
5. If `artifacts/final_test_ml_predictions.json` exists, use it as the ML prior.
6. Generate Claude predictions for all `(person_id, question_id)` rows in the final test.

Modeling guidance:
- Treat `Personal Background` and `Political & Social Views` as direct prediction tasks.
- Treat `News Sharing Behavior` and `Information Sharing Behavior` as the main source of ranking signal.
- Treat `News Sharing Preferences` and `Media Trust & Accuracy` as derived from the behavior blocks, not as isolated standalone questions.
- For `News Sharing Preferences` and `Media Trust & Accuracy`, follow `CLAUDE_WEAK_BLOCK_INSTRUCTIONS.md` closely.
- For media trust, focus more on source ordering and relative trust than on inventing precise raw numbers.
- For news-sharing preferences, infer latent motives first:
  - source sensitivity
  - likes/social-proof sensitivity
  - entertainment vs accuracy preference
  - political-lean sensitivity
  - headline sensitivity

Output requirements:
- Create exactly one prediction per `(person_id, question_id)`.
- Use only valid answer ranges.
- Output numeric answers, not strings.
- Save the final result as:
  - `artifacts/claude_predictions.json`

Required JSON format:
[
  {
    "person_id": "example_id",
    "question_id": "T1",
    "predicted_answer": 3
  }
]

Behavior rules:
- Do not change the project code unless necessary.
- Do not rewrite the ML pipeline.
- Your main goal is to create strong Claude predictions that can later be blended with ML using `blend_claude_with_ml.py`.
- If the ML prior appears over-smoothed, move away from it to restore ranking signal, especially in:
  - `T45-T57`
  - `T74-T76`
  - `T77-T84`

After generating `artifacts/claude_predictions.json`, briefly report:
- that the file was created
- whether all rows were covered
- any weak spots or assumptions
